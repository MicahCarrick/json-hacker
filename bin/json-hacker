#!/usr/bin/env python
import logging
import wxversion
wxversion.select('2.9')
from jsonhacker import JsonHacker

if __name__ == "__main__":
    # setup logging
    logging.basicConfig(level=logging.DEBUG)
    
    
    # launch the application
    app = JsonHacker()
    app.MainLoop()
